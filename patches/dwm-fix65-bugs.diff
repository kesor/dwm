--- a/dwm.c
+++ b/dwm.c
@@ -474,12 +474,18 @@ cleanup(void)
 	Layout foo = { "", NULL };
 	Monitor *m;
 	size_t i;
+	Client *current, *next;
 
 	view(&a);
 	selmon->lt[selmon->sellt] = &foo;
-	for (m = mons; m; m = m->next)
-		while (m->stack)
-			unmanage(m->stack, 0);
+	for (m = mons; m; m = m->next) {
+		current = m->stack;
+		while (current) {
+			next = current->snext;
+			unmanage(current, 0);
+			current = next;
+		}
+	}
 	XUngrabKey(dpy, AnyKey, AnyModifier, root);
 	while (mons)
 		cleanupmon(mons);
@@ -500,11 +506,14 @@ cleanupmon(Monitor *mon)
 {
 	Monitor *m;
 
+	if (!mons)
+		return;
 	if (mon == mons)
 		mons = mons->next;
 	else {
 		for (m = mons; m && m->next != mon; m = m->next);
-		m->next = mon->next;
+		if (m)
+			m->next = mon->next;
 	}
 	XUnmapWindow(dpy, mon->barwin);
 	XDestroyWindow(dpy, mon->barwin);
@@ -1704,7 +1713,10 @@ tile(Monitor *m)
 			if (my + HEIGHT(c) < m->wh)
 				my += HEIGHT(c);
 		} else {
-			h = (m->wh - ty) / (n - i);
+			if (n > i) // prevent division by zero
+				h = (m->wh - ty) / (n - i);
+			else
+				h = m->wh - ty; // default to height remaining
 			resize(c, m->wx + mw, m->wy + ty, m->ww - mw - (2*c->bw), h - (2*c->bw), 0);
 			if (ty + HEIGHT(c) < m->wh)
 				ty += HEIGHT(c);
@@ -1909,7 +1921,7 @@ updategeom(void)
 		/* removed monitors if n > nn */
 		for (i = nn; i < n; i++) {
 			for (m = mons; m && m->next; m = m->next);
-			while ((c = m->clients)) {
+			while (m && (c = m->clients)) {
 				dirty = 1;
 				m->clients = c->next;
 				detachstack(c);
--- a/drw.c
+++ b/drw.c
@@ -119,6 +119,12 @@ xfont_create(Drw *drw, const char *fontn
 		die("no font specified.");
 	}
 
+	if (!xfont) {  // Final check to prevent null dereference
+		fprintf(stderr, "error, failed to load font.\n");
+		FcPatternDestroy(pattern);  // Clean up pattern if allocated
+		return NULL;
+	}
+
 	font = ecalloc(1, sizeof(Fnt));
 	font->xfont = xfont;
 	font->pattern = pattern;
@@ -263,7 +269,7 @@ drw_text(Drw *drw, int x, int y, unsigne
 	if (!invalid_width && render)
 		invalid_width = drw_fontset_getwidth(drw, invalid);
 	while (1) {
-		ew = ellipsis_len = utf8err = utf8charlen = utf8strlen = 0;
+		ew = ellipsis_len = utf8err = utf8strlen = 0;
 		utf8str = text;
 		nextfont = NULL;
 		while (*text) {
@@ -345,9 +351,10 @@ drw_text(Drw *drw, int x, int y, unsigne
 			fccharset = FcCharSetCreate();
 			FcCharSetAddChar(fccharset, utf8codepoint);
 
-			if (!drw->fonts->pattern) {
+			if (!drw->fonts || !drw->fonts->pattern) {
 				/* Refer to the comment in xfont_create for more information. */
 				die("the first font in the cache must be loaded from a font string.");
+				return 0;
 			}
 
 			fcpattern = FcPatternDuplicate(drw->fonts->pattern);
