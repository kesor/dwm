--- a/dwm.c
+++ b/dwm.c
@@ -510,12 +510,18 @@ cleanup(void)
 	Layout foo = { "", NULL };
 	Monitor* m;
 	size_t i;
+	Client *current, *next;
 
 	view(&a);
 	selmon->lt[selmon->sellt] = &foo;
-	for (m = mons; m; m = m->next)
-		while (m->stack)
-			unmanage(m->stack, 0);
+	for (m = mons; m; m = m->next) {
+		current = m->stack;
+		while (current) {
+			next = current->snext;
+			unmanage(current, 0);
+			current = next;
+		}
+	}
 	XUngrabKey(dpy, AnyKey, AnyModifier, root);
 	while (mons)
 		cleanupmon(mons);
@@ -536,12 +542,15 @@ cleanupmon(Monitor* mon)
 {
 	Monitor* m;
 
+	if (!mons)
+		return;
 	if (mon == mons)
 		mons = mons->next;
 	else {
 		for (m = mons; m && m->next != mon; m = m->next)
 			;
-		m->next = mon->next;
+		if (m)
+			m->next = mon->next;
 	}
 	XUnmapWindow(dpy, mon->barwin);
 	XDestroyWindow(dpy, mon->barwin);
@@ -723,15 +732,22 @@ dirtomon(int dir)
 {
 	Monitor* m = NULL;
 
+	if (!mons)
+		return NULL;
+	if (!selmon)
+		return mons;
 	if (dir > 0) {
 		if (!(m = selmon->next))
 			m = mons;
-	} else if (selmon == mons)
+	} else if (selmon == mons) {
 		for (m = mons; m->next; m = m->next)
 			;
-	else
-		for (m = mons; m->next != selmon; m = m->next)
+	} else {
+		for (m = mons; m->next && m->next != selmon; m = m->next)
 			;
+		if (!m->next) /* selmon not in monitor list */
+			return mons;
+	}
 	return m;
 }
 
@@ -890,7 +906,7 @@ focusstack(const Arg* arg)
 			for (c = selmon->clients; c && !ISVISIBLE(c); c = c->next)
 				;
 	} else {
-		for (i = selmon->clients; i != selmon->sel; i = i->next)
+		for (i = selmon->clients; i && i != selmon->sel; i = i->next)
 			if (ISVISIBLE(i))
 				c = i;
 		if (!c)
@@ -1749,7 +1765,10 @@ tile(Monitor* m)
 			if (my + HEIGHT(c) < m->wh)
 				my += HEIGHT(c);
 		} else {
-			h = (m->wh - ty) / (n - i);
+			if (n > i)
+				h = (m->wh - ty) / (n - i);
+			else
+				h = m->wh - ty;
 			resize(c, m->wx + mw, m->wy + ty, m->ww - mw - (2 * c->bw), h - (2 * c->bw), 0);
 			if (ty + HEIGHT(c) < m->wh)
 				ty += HEIGHT(c);
@@ -1921,6 +1940,8 @@ updategeom(void)
 		XineramaScreenInfo* info   = XineramaQueryScreens(dpy, &nn);
 		XineramaScreenInfo* unique = NULL;
 
+		if (!info)
+			nn = 0;
 		for (n = 0, m = mons; m; m = m->next, n++)
 			;
 		/* only consider unique geometries as separate screens */
@@ -1954,7 +1975,7 @@ updategeom(void)
 		for (i = nn; i < n; i++) {
 			for (m = mons; m && m->next; m = m->next)
 				;
-			while ((c = m->clients)) {
+			while (m && (c = m->clients)) {
 				dirty      = 1;
 				m->clients = c->next;
 				detachstack(c);
--- a/drw.c
+++ b/drw.c
@@ -121,6 +121,12 @@ xfont_create(Drw* drw, const char* fontn
 		die("no font specified.");
 	}
 
+	if (!xfont) {
+		fprintf(stderr, "error, failed to load font.\n");
+		FcPatternDestroy(pattern);
+		return NULL;
+	}
+
 	font          = ecalloc(1, sizeof(Fnt));
 	font->xfont   = xfont;
 	font->pattern = pattern;
@@ -288,11 +294,13 @@ drw_text(Drw* drw, int x, int y, unsigne
 	if (!invalid_width && render)
 		invalid_width = drw_fontset_getwidth(drw, invalid);
 	while (1) {
-		ew = ellipsis_len = utf8err = utf8charlen = utf8strlen = 0;
-		utf8str                                                = text;
-		nextfont                                               = NULL;
+		ew = ellipsis_len = utf8err = utf8strlen = 0;
+
+		utf8str  = text;
+		nextfont = NULL;
 		while (*text) {
 			utf8charlen = utf8decode(text, &utf8codepoint, &utf8err);
+			charexists  = 0;
 			for (curfont = drw->fonts; curfont; curfont = curfont->next) {
 				charexists = charexists || XftCharExists(drw->dpy, curfont->xfont, utf8codepoint);
 				if (charexists) {
@@ -326,8 +334,6 @@ drw_text(Drw* drw, int x, int y, unsigne
 
 			if (overflow || !charexists || nextfont || utf8err)
 				break;
-			else
-				charexists = 0;
 		}
 
 		if (utf8strlen) {
@@ -348,16 +354,11 @@ drw_text(Drw* drw, int x, int y, unsigne
 		if (render && overflow)
 			drw_text(drw, ellipsis_x, y, ellipsis_w, h, 0, "...", invert);
 
-		if (!*text || overflow) {
+		if (!*text || overflow)
 			break;
-		} else if (nextfont) {
-			charexists = 0;
-			usedfont   = nextfont;
-		} else {
-			/* Regardless of whether or not a fallback font is found, the
-			 * character must be drawn. */
-			charexists = 1;
-
+		else if (nextfont)
+			usedfont = nextfont;
+		else {
 			hash = (unsigned int) utf8codepoint;
 			hash = ((hash >> 16) ^ hash) * 0x21F0AAAD;
 			hash = ((hash >> 15) ^ hash) * 0xD35A2D97;
@@ -370,9 +371,10 @@ drw_text(Drw* drw, int x, int y, unsigne
 			fccharset = FcCharSetCreate();
 			FcCharSetAddChar(fccharset, utf8codepoint);
 
-			if (!drw->fonts->pattern) {
+			if (!drw->fonts || !drw->fonts->pattern) {
 				/* Refer to the comment in xfont_create for more information. */
 				die("the first font in the cache must be loaded from a font string.");
+				return 0;
 			}
 
 			fcpattern = FcPatternDuplicate(drw->fonts->pattern);
