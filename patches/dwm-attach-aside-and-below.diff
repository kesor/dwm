--- a/dwm.c
+++ b/dwm.c
@@ -49,7 +49,8 @@
 #define CLEANMASK(mask)         (mask & ~(numlockmask|LockMask) & (ShiftMask|ControlMask|Mod1Mask|Mod2Mask|Mod3Mask|Mod4Mask|Mod5Mask))
 #define INTERSECT(x, y, w, h, m)    (MAX(0, MIN((x)+(w),(m)->wx+(m)->ww) - MAX((x),(m)->wx)) \
                                * MAX(0, MIN((y)+(h),(m)->wy+(m)->wh) - MAX((y),(m)->wy)))
-#define ISVISIBLE(C)            ((C->tags & C->mon->tagset[C->mon->seltags]))
+#define ISVISIBLEONTAG(C, T)    ((C->tags & T))
+#define ISVISIBLE(C)            ISVISIBLEONTAG(C, C->mon->tagset[C->mon->seltags])
 #define MOUSEMASK               (BUTTONMASK|PointerMotionMask)
 #define WIDTH(X)                ((X)->w + 2 * (X)->bw)
 #define HEIGHT(X)               ((X)->h + 2 * (X)->bw)
@@ -198,6 +199,7 @@ static int applysizehints(Client* c, int
 static void arrange(Monitor* m);
 static void arrangemon(Monitor* m);
 static void attach(Client* c);
+static void attachBelow(Client* c);
 static void attachstack(Client* c);
 static void buttonpress(XEvent* e);
 static void checkotherwm(void);
@@ -238,6 +240,7 @@ static void maprequest(XEvent* e);
 static void monocle(Monitor* m);
 static void motionnotify(XEvent* e);
 static void movemouse(const Arg* arg);
+static Client* nexttagged(Client* c);
 static Client* nexttiled(Client* c);
 static void pop(Client* c);
 static void propertynotify(XEvent* e);
@@ -497,6 +500,28 @@ attach(Client* c)
 }
 
 void
+attachBelow(Client* c)
+{
+	// If there is nothing on the monitor or the selected client is floating, attach as normal
+	if (c->mon->sel == NULL || c->mon->sel->isfloating) {
+		Client* at = nexttagged(c);
+		if (!at) {
+			attach(c);
+			return;
+		}
+		c->next  = at->next;
+		at->next = c;
+		return;
+	}
+
+	// Set the new client's next property to the same as the currently selected clients next
+	c->next = c->mon->sel->next;
+	// Set the currently selected clients next property to the new client
+	c->mon->sel->next = c;
+}
+
+
+void
 attachstack(Client* c)
 {
 	c->snext      = c->mon->stack;
@@ -1271,7 +1296,7 @@ manage(Window w, XWindowAttributes* wa)
 		c->isfloating = c->oldstate = trans != None || c->isfixed;
 	if (c->isfloating)
 		XRaiseWindow(dpy, c->win);
-	attach(c);
+	attachBelow(c);
 	attachstack(c);
 	XChangeProperty(dpy, root, netatom[NetClientList], XA_WINDOW, 32, PropModeAppend,
 	                (unsigned char*) &(c->win), 1);
@@ -1404,6 +1429,17 @@ movemouse(const Arg* arg)
 }
 
 Client*
+nexttagged(Client* c)
+{
+	Client* walked = c->mon->clients;
+	for (;
+	     walked && (walked->isfloating || !ISVISIBLEONTAG(walked, c->tags));
+	     walked = walked->next)
+		;
+	return walked;
+}
+
+Client*
 nexttiled(Client* c)
 {
 	for (; c && (c->isfloating || !ISVISIBLE(c)); c = c->next)
@@ -1643,7 +1679,7 @@ sendmon(Client* c, Monitor* m)
 	clearclientfrompertagsel(c);
 	c->mon  = m;
 	c->tags = m->tagset[m->seltags]; /* assign tags of target monitor */
-	attach(c);
+	attachBelow(c);
 	attachstack(c);
 	setclienttagprop(c);
 	if (c->isfullscreen)
@@ -2379,47 +2415,53 @@ updategeom(void)
 		XFree(info);
 		nn = j;
 
-		/* new monitors if nn > n */
-		for (i = n; i < nn; i++) {
-			for (m = mons; m && m->next; m = m->next)
-				;
-			if (m)
-				m->next = createmon();
-			else
-				mons = createmon();
-		}
-		for (i = 0, m = mons; i < nn && m; m = m->next, i++) {
-			if (i >= n || unique[i].x_org != m->mx || unique[i].y_org != m->my || unique[i].width != m->mw || unique[i].height != m->mh) {
-				dirty  = 1;
-				m->num = i;
-				/* this is ugly, but it is a race condition otherwise */
-				snprintf(m->monmark, sizeof(m->monmark), "(%d)", m->num + 1);
-				m->mx = m->wx = unique[i].x_org;
-				m->my = m->wy = unique[i].y_org;
-				m->mw = m->ww = unique[i].width;
-				m->mh = m->wh = unique[i].height;
-				updatebarpos(m);
+		/* keep current layout if Xinerama reports no usable screens */
+		if (nn > 0) {
+			/* new monitors if nn > n */
+			for (i = n; i < nn; i++) {
+				for (m = mons; m && m->next; m = m->next)
+					;
+				if (m)
+					m->next = createmon();
+				else
+					mons = createmon();
 			}
-			if (i == statmonval)
-				statmon = m;
-		}
-		/* removed monitors if n > nn */
-		for (i = nn; i < n; i++) {
-			for (m = mons; m && m->next; m = m->next)
-				;
-			while (m && (c = m->clients)) {
-				dirty      = 1;
-				m->clients = c->next;
-				detachstack(c);
-				c->mon = mons;
-				attach(c);
-				attachstack(c);
+			for (i = 0, m = mons; i < nn && m; m = m->next, i++) {
+				if (i >= n || unique[i].x_org != m->mx || unique[i].y_org != m->my || unique[i].width != m->mw || unique[i].height != m->mh) {
+					dirty  = 1;
+					m->num = i;
+					/* this is ugly, but it is a race condition otherwise */
+					snprintf(m->monmark, sizeof(m->monmark), "(%d)", m->num + 1);
+					m->mx = m->wx = unique[i].x_org;
+					m->my = m->wy = unique[i].y_org;
+					m->mw = m->ww = unique[i].width;
+					m->mh = m->wh = unique[i].height;
+					updatebarpos(m);
+				}
+				if (i == statmonval)
+					statmon = m;
+			}
+			/* removed monitors if n > nn */
+			for (i = nn; i < n; i++) {
+				for (m = mons; m && m->next; m = m->next)
+					;
+				while (m && (c = m->clients)) {
+					dirty      = 1;
+					m->clients = c->next;
+					detachstack(c);
+					clearclientfrompertagsel(c);
+					c->mon = mons;
+					attachBelow(c);
+					attachstack(c);
+					if (c->isfullscreen)
+						resizeclient(c, mons->mx, mons->my, mons->mw, mons->mh);
+				}
+				if (m == selmon)
+					selmon = mons;
+				if (m == statmon)
+					statmon = mons;
+				cleanupmon(m);
 			}
-			if (m == selmon)
-				selmon = mons;
-			if (m == statmon)
-				statmon = mons;
-			cleanupmon(m);
 		}
 		free(unique);
 	} else
@@ -2439,8 +2481,16 @@ updategeom(void)
 	if (!statmon)
 		statmon = mons;
 	if (dirty) {
+		Monitor* mm;
+		Client* cc;
+
 		selmon = mons;
 		selmon = wintomon(root);
+		/* keep fullscreen clients covering their monitor after RandR reshuffles */
+		for (mm = mons; mm; mm = mm->next)
+			for (cc = mm->clients; cc; cc = cc->next)
+				if (cc->isfullscreen)
+					resizeclient(cc, mm->mx, mm->my, mm->mw, mm->mh);
 	}
 	return dirty;
 }
